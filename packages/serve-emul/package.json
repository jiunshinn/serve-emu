{
  "name": "serve-emul",
  "version": "0.0.5",
  "description": "The `npx serve` of Android devices — stream a booted emulator or attached device over a WebSocket H.264 feed decoded in the browser with WebCodecs.",
  "license": "Apache-2.0",
  "type": "module",
  "author": "jiunshinn",
  "homepage": "https://github.com/jiunshinn/serve-emul",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jiunshinn/serve-emul.git",
    "directory": "packages/serve-emul"
  },
  "bugs": {
    "url": "https://github.com/jiunshinn/serve-emul/issues"
  },
  "keywords": [
    "android",
    "emulator",
    "adb",
    "scrcpy",
    "screen-mirror",
    "remote-control",
    "h264",
    "webcodecs",
    "bun"
  ],
  "engines": {
    "bun": ">=1.3.13"
  },
  "bin": {
    "serve-emul": "src/cli.ts"
  },
  "exports": {},
  "scripts": {
    "setup": "bun run scripts/fetch-scrcpy.ts && bun run build:ui",
    "dev": "bun --watch run src/cli.ts",
    "dev:ui": "vite",
    "start": "bun run src/cli.ts",
    "build:ui": "vite build",
    "build": "bun run build:ui",
    "docs:sync": "bun run scripts/sync-readme.ts --write",
    "docs:check": "bun run scripts/sync-readme.ts --check",
    "test": "bun test",
    "coverage": "bun test --coverage --coverage-reporter=text --coverage-reporter=lcov --coverage-dir=coverage && bun run scripts/check-coverage.ts",
    "test:package": "bun run scripts/package-smoke.ts",
    "check:unused": "knip",
    "check": "bun run docs:check && bun run coverage && bun run typecheck && bun run typecheck:ui && bun run typecheck:tests && bun run build && bun run test:package",
    "release": "bun run scripts/release.ts",
    "typecheck": "tsc --noEmit",
    "typecheck:ui": "tsc -p src/ui --noEmit",
    "typecheck:tests": "tsc -p tsconfig.tests.json --noEmit",
    "prepublishOnly": "bun run check",
    "test:browser": "playwright test"
  },
  "files": [
    "src",
    "scripts",
    "vendor",
    "dist/ui",
    "docs",
    "CHANGELOG.md",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@fastify/busboy": "^3.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "knip": "6.26.0",
    "vite": "^5.4.11"
  }
}
